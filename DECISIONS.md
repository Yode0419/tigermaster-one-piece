# 決策記錄（專案層級）

一行一則，記錄使用者修正過或在選項間拍板的決定，避免跨 session 反覆。

- 2026-08-17: 新增 `/robin` skill，負責「已完成的功能 exploration → wiki 產品知識」同步。Why: exploration 探索過程混亂，需要有人把最終真相萃取進 wiki，並留下決策脈絡供日後回溯。
- 2026-08-17: `/robin` 產出兩份東西——wiki 正式文件（只寫現況、現在式）＋ `decision-summary.md` 留在原 exploration 功能資料夾。Why: 兩種讀者需求不同，想知道「怎麼運作」的人不該被迫讀四月的會議議程。
- 2026-08-17: `docs/exploration/` 改為三個實體狀態資料夾 `in-progress/`、`completed/`、`on-hold/`，功能資料夾放在其下。Why: 使用者主要從 VSCode 檔案樹找文件，狀態必須在檔案樹上看得見，光靠 INDEX.md 不夠。
- 2026-08-17: 功能資料夾由 `/robin` 負責從 `in-progress/` 搬到 `completed/`，並同時修正因搬移而斷掉的相對連結。Why: 搬移與知識同步是同一件事，交給同一個角色才不會脫節。
- 2026-08-17: `completed/` 的定義是「已上線**且**知識已同步進 wiki」，不是只有上線。Why: 兩者若分開，Robin 就無法用「在不在 in-progress」判斷待辦。
- 2026-08-17: 根 `docs/INDEX.md` 只列三個狀態資料夾，不列個別功能。Why: 最外層是路由層，細節放各自的 INDEX。
- 2026-08-17: 用語用「已完成」，不用「已落地」；INDEX 章節標題不加 emoji。Why: 使用者明確指定。
- 2026-08-17: `/robin` 暫不處理「已擱置」狀態，只做 進行中 → 已完成。Why: 擱置情境不同（不需產出 wiki 文件）且少見，避免範圍蔓延，先手動處理。
